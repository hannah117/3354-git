9. a) 7
   b) Jan 24 2017
   c) git log shows the history of commits in a project and allows you to search
      for changes, where git status allows you to view current details of the working directory
   d) An untracked file is newly introduced file 
      not yet in the repository that needs to be added first, 
      and an unstaged file exists in the directory, but its 
      changes have not been recorded in git.
   e) Checking out will not make changes to the commit history, while reset 
      can be used to return the entire tree to the last committed state. 
   f) You will get an error and warning to commit changes or delete them. You can 
      use stash to save the changes to apply later.

      